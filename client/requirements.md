## Packages
framer-motion | Page transitions and list animations
date-fns | Human-readable date formatting

## Notes
- Relying on REST endpoints as defined in routes.ts
- Assuming /api/plans/generate takes time (OpenAI generation) so implementing an animated loading state.
